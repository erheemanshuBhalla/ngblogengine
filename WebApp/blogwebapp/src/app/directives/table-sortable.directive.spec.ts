//import { TableSortableDirective } from './table-sortable.directive';
import { NgbdSortableHeader } from '../directives/table-sortable.directive';

describe('TableSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbdSortableHeader();
    expect(directive).toBeTruthy();
  });
});
